version: "3.3"
# Указываем раздел со связанными сервисами

services:
  # Раскомментировать, если нужно использовать nginx
  nginx:
    depends_on:
      - nuxt
    image: nginx:latest
    ports:
      - '80:80'
    volumes:
      # Заменить на свой nginx конфиг
      - ./nginx:/etc/nginx/conf.d
      # Монтируем папку с логами на хост машину для более удобного доступа
      - ./logs:/var/log/nginx/
    restart: always

  nuxt:
    build: ./
    expose:
      - '3000'
    restart: always